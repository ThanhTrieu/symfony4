{% if news_list is defined %}
    {% for key, item in news_list %}
        <li id="item-{{ item.id }}" class="{{ (key+1)%3==0? 'last':'' }}">
            <a href="{{ item.url }}" class="img-sp" title="{{ item.title }}">
                <img src="{{ item.avatar }}" alt="{{ item.title }}" />
            </a>
            <div class="info-car">
                <h3><a href="{{ item.url }}" class="name-pro"
                       title="{{ item.title }}">{{ item.title }}</a></h3>
                <span class="ic-user">
                            <i class="icon-user"></i>
                            Bruce Nguyen
                        </span>
                <span class="mg-9-10">|</span>
                <span class="date-creat">
                            <i class="icon-clock"></i> {{ item.s_publish_time }}
                        </span>
                <div class="short-info">
                    {{ item.sapo }}
                </div>
            </div>
        </li>
    {% endfor %}
{% endif %}
